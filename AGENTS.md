# BPB_offline_mod

Offline Backpack Battles leaderboard: seeder + game mod. See `CONTEXT.md` for glossary.

## Agent skills

### Issue tracker

GitHub Issues at `vadash/BPB_offline_mod` via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
